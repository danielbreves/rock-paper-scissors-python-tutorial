"""
You're on a roll! Level 3 awaits!

We need the computer to pick its own hand. To do this, we'll use Python's "random" module.

A module is like a toolbox that someone else has already built for us.
Python comes with lots of modules, and "random" is one that helps us pick things randomly.
To use a module, we need to "import" it first.

We also need to learn about lists! A list is a collection of items inside square brackets.
For example: ["apple", "banana", "cherry"] is a list of three fruits.
"""

# Step 1: Let's import the random module. Write "import random" below this line:


# Step 2: Let's make a list of the possible options.
# Write options = ["rock", "paper", "scissors"] below this line:


# Step 3: Now let's make the computer pick a random item from our list.
# Write computer_hand = random.choice(options) below this line:


# Step 4: Print the computer's choice to see what it picked.
# Write print("The computer chose " + computer_hand) below this line:


# Run the code with "python level-3.py" to see what the computer picks!

# Keep going! Open level-4.py!
